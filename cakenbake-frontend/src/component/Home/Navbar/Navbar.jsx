import React from 'react';
import './Navbar.css';

const Navbar = () => {
    return (
        <nav className="navbar">
            <div className="navbar-container">
                {/* Content to be added in later commits */}
            </div>
        </nav>
    );
};

export default Navbar;
